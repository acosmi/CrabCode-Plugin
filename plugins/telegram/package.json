{
  "name": "crabcode-channel-telegram",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./src/server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/server.ts --outdir dist --target bun --format esm"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "grammy": "^1.21.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3"
  }
}
