# 代码化简

CrabCode 代理插件:在保持功能完全不变的前提下化简与打磨代码,提升清晰度、一致性与可维护性,默认聚焦最近改动的代码。

## 安装

在 CrabCode 插件市场中添加 `code-simplifier`,或通过 `/plugin` 安装。

## 代理

| 代理 | 说明 |
|---|---|
| `code-simplifier` | 化简代码:遵循项目既有规范(模块与导入风格、命名、错误处理),只改写法不改行为,并避免过度压缩牺牲可读性 |

## 使用入口

在完成一段编码后说"化简一下刚才的改动"或"让这段代码更可读",即可触发本代理。
