{
  "name": "hookify",
  "version": "0.1.0",
  "description": "CrabCode hookify plugin (TypeScript port).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/hooks/preToolUse.ts src/hooks/postToolUse.ts src/hooks/stop.ts src/hooks/userPromptSubmit.ts --outdir dist --target bun --format esm"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3"
  }
}
