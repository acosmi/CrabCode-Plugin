# Third-party notices

This plugin re-implements the behavior of the upstream `hookify` plugin from the upstream
marketplace cached under `bangong/`. The upstream source is MIT-licensed. The upstream `LICENSE`
text is not redistributed inside this product directory; the upstream MIT terms apply to any
portions ported verbatim into `src/`.

The TypeScript rule engine, simple-YAML frontmatter parser, and rule file format follow the
upstream specification. Command and skill text was rewritten to remove product-facing references
to upstream identifiers per the CrabCode migration rules.
