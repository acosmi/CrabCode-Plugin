# Future MCP Review Checklist

Status: `blocked / non-executable / not connected / not tested`

Record the capability outcome, owner, data/privilege boundary, identity and
consent model, destructive approvals, fail-closed behavior, and safe fallback.

Evidence remains missing until supplied by a future approved task:

- immutable source and artifact provenance;
- threat model and dependency/license review;
- host activation, isolation, and negative tests;
- provider-specific authentication and redirect E2E;
- isolated protocol/tool-discovery tests;
- upgrade plus CrabCode restart and rollback E2E;
- deterministic release and public-byte verification.

Do not add implementation fields or connection instructions to this checklist.
