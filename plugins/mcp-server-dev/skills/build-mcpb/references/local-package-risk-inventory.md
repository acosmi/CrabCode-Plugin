# Future Local MCP Package Risk Inventory

Status: `blocked / non-executable / not packaged / not installed / not tested`

Record why local execution is necessary; filesystem/device/desktop/network and
subprocess privileges; path/symlink/sandbox controls; destructive approvals;
secret handling; dependency provenance; licenses; vulnerability review;
archive extraction; signature trust; cache binding; upgrade/restart; rollback;
and deterministic release evidence.

Leave manifest, runtime, entry point, command, arguments, environment, package,
signature, and installation fields unset.
