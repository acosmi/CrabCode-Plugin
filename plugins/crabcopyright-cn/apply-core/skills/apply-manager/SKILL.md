---
name: apply-manager
description: 软著申请管家——统筹中国版权保护中心软件著作权登记的全流程,规划步骤、判断用户所处阶段、把每一步分派给对应的子技能(材料清单/源代码鉴别材料/说明书鉴别材料/一致性校验/申请包生成/在线填报),并守住全局一致性红线。当用户提到"申请软著""软著申请""软件著作权""软件著作权登记""版权登记""软著登记""准备软著材料""软著材料""帮我把项目登记软著"等,或想为一套源代码办理软件著作权时,使用本技能作为总入口——即使用户没说"技能"二字。不处理专利、商标、作品著作权(文字/美术/音乐)登记。
argument-hint: "[软件全称] [版本号] [源代码目录] [说明书路径] [申请人类型:个人/企业/事业单位]"
allowed-tools:
  - Read
  - Write
  - Glob
  - Grep
  - Task
  - AskUserQuestion
  - WebSearch
  - WebFetch
---

# 软著申请管家（总编排）

统筹一次完整的软件著作权登记申请,产出一套可提交至**中国版权保护中心**
(register.ccopyright.com.cn) 的电子材料包。本技能是伞状插件的入口与调度中枢:
自己不埋头做具体材料,而是规划全局、判断用户卡在哪一步、把活分派给专职子技能。

**开工前先读** `${CRABCODE_PLUGIN_ROOT}/apply-core/GUIDE.md`(官方规范速查)——所有页数、
行数、日期、红线判断都以它为准,不要凭记忆另编一套。工序间参数交接读写
`${CRABCODE_PLUGIN_ROOT}/apply-core/MANIFEST.md` 定义的 `outputs/<申请名>/manifest.json`,
不靠口头复述。

## 开场依赖自检（第一步就做,别拖到排版才失败）

工序 2/3 的 PDF 排版全部依赖 **crabcode-office-suite** 插件
(`crabcode-office-suite:crabcode-pdf` / `crabcode-office-suite:crabcode-documents` 技能)。
开场先探测它是否可用:检查当前可用技能列表中是否存在
`crabcode-office-suite:crabcode-pdf` 与 `crabcode-office-suite:crabcode-documents`
(即办公套件已安装并启用)。不可用、或触发时报 Unknown skill,说明办公套件未安装:
**立即告知用户**"源代码/说明书转规范 PDF 的环节需要办公套件,请先通过 `/plugin`
从 marketplace 安装启用 `crabcode-office-suite` 后重试",并问清是现在装还是先推进
不依赖它的工序(0/1/4/6),不要走到排版环节才报错。

## 输入（缺则用 AskUserQuestion 索取）

| 参数 | 说明 | 示例 |
|------|------|------|
| `$SOFTWARE_NAME` | 软件全称 | `晶石进销存管理系统` |
| `$VERSION` | 版本号(`V1.0` 或 `1.0`,全材料统一) | `V1.0` |
| `$SOURCE_CODE_DIR` | 源代码根目录 | `/path/to/project` |
| `$MANUAL_PATH` | 说明书 doc/docx 路径(可后补) | `/path/to/manual.docx` |
| `$APPLICANT_TYPE` | 申请人类型 | `个人`/`企业`/`事业单位` |

## 最终产物

```
outputs/软著申请-${SOFTWARE_NAME}${VERSION}/
├── manifest.json                   （工序交接单,结构见 MANIFEST.md）
├── 01-软件著作权登记申请表.pdf     （用户在平台生成后放入）
├── 02-源代码鉴别材料.pdf           （前30+后30页,页眉含名称+版本号）
├── 03-说明书鉴别材料.pdf           （封面/目录/概述/功能截图/运行环境）
├── 04-身份证明文件.pdf             （用户自备）
├── 05-其他材料/                    （合作/委托/二次开发等补充）
├── 中间态/                         （源码材料文本、说明书定稿 docx、功能说明 txt）
└── 材料自查对照表.md               （由 scripts/check_all.py 结果产出）
```

## 编排流程（判断阶段 → 点名下一道工序）

先弄清用户已经到哪一步,再从对应环节切入;不必每次都从头走。本技能不直接代跑
子技能,而是**按顺序点名下一道工序并交接参数**——各子技能都有自己的触发词,会在
对应场景被唤起。标准顺序:

0. **申请板块规划** → 转 **application-planning** 技能
   若源码是 monorepo/大项目,或用户想登记**多个**软著,**先做这步**:分析仓库结构,
   按第一性原理规划拆成几个申请、每个申请归属哪些源码目录、说明书截哪些前端页面
   (含开发环境访问地址),并为每个申请初始化 `outputs/<申请名>/manifest.json`。
   每个申请是独立的(全称+版本),彼此不共用页眉名称、不重复计入公共代码。
   单一软件可跳过此步,由本技能直接初始化 manifest。

   **多软著批量走 fan-out,不在主会话串行硬扛**:规划确认后,用 Task 工具为**每个
   申请**派发一个独立子代理并行推进只读准备工作——各子代理限定在自己申请的源码
   目录内,完成材料清单核算、源码文件挑选与行数统计(职责同 `sc-material-collector`)、
   截图取证清单细化(职责同 `manual-evidence-collector`),以 manifest 草稿(JSON)
   回传;**子代理一律只读、不落盘**。主会话逐份核对草稿后写入各自的
   `outputs/<申请名>/manifest.json`,再逐申请执行需要落盘的工序(3 排版、5 归档),
   最后派发 `filing-reviewer` 对全部申请做一致性+跨申请查重的对抗式审查(见下文
   "子代理与信任隔离")。

1. **材料清单核查** → 转 **materials-checklist** 技能
   按 `$APPLICANT_TYPE` 和开发方式,列出该情形所需全部材料与缺口。**先做这步**,
   因为它决定后面要不要准备许可证明、合同等补充件。

2. **源代码鉴别材料** → 转 **source-code-material** 技能
   扫描 `$SOURCE_CODE_DIR`,生成 60 页规范 PDF。

3. **说明书鉴别材料** → 转 **manual-material** 技能
   处理 `$MANUAL_PATH`,引导调用 crabcode-office-suite 办公套件生成规范 PDF。

4. **一致性校验** → 转 **consistency-check** 技能
   对 manifest 与中间态材料机械比对名称/版本号/日期逐字一致。**每当有材料更新就应重跑。**

5. **申请包生成** → 转 **package-build** 技能
   统一命名归档 + 运行 `scripts/check_all.py` 产出材料自查对照表。

6. **在线填报指导** → 转 **filing-guide** 技能
   给出平台逐字段填报卡片与 2026 新版申请表提示。

工序间交接**读写 manifest,不靠口头复述**:每道工序开工先读
`outputs/<申请名>/manifest.json` 取参数,完工把自己的产出字段与 `steps` 状态写回
(读写职责表见 MANIFEST.md);子技能发现 manifest 缺字段才向用户追问。本技能靠
manifest 的 `steps` 判断用户到哪一步、汇报进度、决定下一步。

## 子代理与信任隔离（仿 crablaw 尽调三层）

本插件在 `agents/` 提供三个子代理,用 Task 派发,分工与权限如下:

| 子代理 | 角色 | 权限 |
|--------|------|------|
| `sc-material-collector` | 收集:扫仓库挑源码文件、统计行数、出候选清单 | 只读 |
| `manual-evidence-collector` | 收集(可选):核对细化说明书截图取证清单 | 只读 |
| `filing-reviewer` | 审查:独立做一致性校验+跨申请查重,调用 scripts/ 脚本,只报告不修改 | 只读 |

**落盘只发生在主会话**:manifest 由主会话各工序写入;最终材料仅由 package-build
工序(主会话执行)写入 `outputs/`。子代理回传的一切结果都当**待核数据**,主会话核对
后才写入 manifest;审查者(filing-reviewer)与生成者相互独立,它的问题清单不能由
生成工序自行"解释掉",须逐条修复或由用户明示接受。

## 全局红线（贯穿所有子技能,反复守）

1. **一致性第一**:软件全称、版本号、开发完成日期在申请表/源码页眉/说明书三处**逐字一致**(含大小写、空格、有无"V")。这是最高频驳回原因。
2. **真实不注水**:绝不伪造、PS、虚构;不用空行/纯注释/模板凑行数页数。
3. **日期自洽**:开发完成日期 ≤ 首次发表日期;企业申请不早于成立日期。
4. **2026 新版申请表**(以平台实际为准,非《办法》条文):经办人本人签字+身份证号、手抄"未使用 AI"诚信承诺、功能说明扩写至 500–1300 字、失信/征信惩戒。详见 GUIDE.md §8。
5. **隐私**:产物中不留身份证号等,由用户自行填入平台。
6. **免费**:官方普通登记自 2017-04-01 起免费,提醒用户勿被"官费"话术误导(代理费另计)。

## 交付

每完成一个阶段,向用户输出:已完成项、产物路径、待办清单(✅/❌/⚠️)、以及下一步建议。
全流程结束时提示:申请表须在平台自行生成填报,身份证明由用户放入,经办人须本人签字。
