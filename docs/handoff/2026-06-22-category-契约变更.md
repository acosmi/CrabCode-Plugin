# 下游契约变更:marketplace 的 `category` 大类重分类(15 → 18)

- **日期**: 2026-06-22
- **基准**: `main @ 43390df` 之上的 `task/marketplace-recategorize-20260622`
- **来源文件**: `.crabcode-plugin/marketplace.json`
- **目的**: 向消费 marketplace 的下游(前端导航/分组)交接 `category` 大类取值变更,请下游更新「category id → 导航大类」映射。
- **同级前序**: `docs/handoff/2026-06-21-下游字段契约.md`(tier/groups)。

> 本变更为**纯数据**:仅改 73 个条目中 14 条的 `category` 字段值,不动其他字段、不改运行时代码、不破三校验(`category` 在校验器中无白名单约束)。

---

## 变更摘要

大类总数 **15 → 18**:新增 3、改名 1、删除 0。

### 新增 3 个 category id
| id | 中文名 | 成员 |
|---|---|---|
| `media-ops` | 媒体运营 | crabcode-media-ops |
| `memory` | 记忆管理 | crabcode-memory-management |
| `code-tools` | 代码工具连接器 | context7, greptile, laravel-boost, serena |

### 改名 1 个(⚠️ id 变更,下游须更新映射键)
| 旧 id | 新 id | 中文名 | 成员 |
|---|---|---|---|
| `work-platform` | `enterprise-workbench` | 企业工作台 | crabwork-productivity, -enterprise-search, -plugin-management, -bio-research, -small-business |

### 受影响的既有 category(成员增减,id 不变)
| id | 变化 | 现成员 |
|---|---|---|
| `showcase` | +2 | cn-legal-study, cwc-makers, math-olympiad, playground, **crabcode-example-plugin**, **crabcode-example-skills** |
| `dev-tools` | -4 连接器 +hookify | agent-sdk-dev, mcp-server-dev, plugin-dev, skill-creator, ai-api-dev, **hookify** |
| `essentials` | -记忆 -2 示例 | crabcode-setup, builder-hub |
| `security` | -hookify | crabcode-security-review, security-guidance |
| `content-office` | -媒体 | frontend-design, crabcode-office-suite |

---

## 完整 18 大类清单(改后)

| category id | 中文名 | 数 |
|---|---|---:|
| `language-server` | 语言服务 | 12 |
| `integrations` | 第三方集成 | 11 |
| `dev-workflow` | 开发工作流 | 8 |
| `showcase` | 示例与演示 | 6 |
| `dev-tools` | 开发工具(扩展) | 6 |
| `enterprise-workbench` | 企业工作台 | 5 |
| `code-tools` | 代码工具连接器 | 4 |
| `biz-ops` | 业务运营 | 4 |
| `go-to-market` | 市场开拓 | 3 |
| `content-office` | 内容与办公 | 2 |
| `essentials` | 基础必备 | 2 |
| `security` | 安全 | 2 |
| `session-style` | 会话风格 | 2 |
| `eng-data` | 工程与数据 | 2 |
| `media-ops` | 媒体运营 | 1 |
| `memory` | 记忆管理 | 1 |
| `legal-workflow` | 法律工作流 | 1 |
| `finance` | 财务 | 1 |

核平:12+11+8+6+6+5+4+4+3+2+2+2+2+2+1+1+1+1 = **73** ✓

---

## 需下游处理的事项

1. **改名映射**:把导航中 `work-platform` 的映射键改为 `enterprise-workbench`(中文「企业工作台」)。这是唯一的 id 重命名,旧 id 不再出现。
2. **新增 3 类**:为 `media-ops` / `memory` / `code-tools` 补导航大类与中文名。
3. **中文名落档**:本仓尚无 `category → 中文名` 映射表(中文名仅存于本交接与方案文档)。若下游需要权威映射表,本仓可后续独立立项新建 `.crabcode-plugin/categories.json` + 校验白名单(方案第 5 节,**本次未做**)。在此之前,以本表为准。
4. **无白名单提醒**:当前 `category` 仍是自由字符串,校验器不校验合法值。新增条目若填错 category 不会报错——下游若依赖 18 类闭集,建议自行做未知 id 的兜底(归「其他」而非崩溃)。
