# 执行日志:分类归并 + Knowledge-Work 插件转化(单会话串行)

- **日期**: 2026-06-21
- **任务分支**: `task/plugin-taxonomy-and-kw-conversion-20260621`
- **方案基准**: `docs/audit/2026-06-21-分类归并方案.md` + `docs/audit/2026-06-21-knowledge-work-插件转化方案.md`

---

## 0. 前置环境冲突与裁决

- 开工时工作区正被 **crablaw-cn 中国法化任务**占用(分支 `task/crablaw-cn-localize-all-20260621`,批次0-3 + 收尾共 5 提交)。
- 触发硬约束「不覆盖其他窗口改动」→ 停下交用户裁决。用户选「等 crablaw-cn 完成再做」。
- crablaw-cn 收口后(工作区干净、5 提交落地、未合并 main),我据此起步。

### Git 拓扑决策(偏离判断第2条 · 已记录)
- **偏离点**: 从已收口的 crablaw-cn 分支(而非旧 main)新建任务分支。
- **原因**: crablaw-cn 的 legal 中国法化 + marketplace 注册在 task 分支未合并 main;从旧 main 起步会拿到缺 legal 条目的旧 marketplace,导致最终合并冲突。
- **影响面**: 我的分支以 crablaw-cn 5 提交为基础;合并 main 时一并带上(若已先合并则自动去重)。
- **红线**: checkout -b 非破坏;绝不改写/删除/回滚 crablaw-cn 任何提交;合并 main 留作终点闸门交用户。

---

## 1. 基线核对(crablaw-cn 收口后实况)

- marketplace 总条目:**75**(方案文档写 66,差异源于 crablaw-cn 新增 legal 板块)。
- crablaw-cn 引入 2 个方案未预料的新单例分类:`legal-education`(cn-legal-study)、`developer-tools`(builder-hub)。

### 新单例归并适配决策(自主 + 记录,域数仍 11)
| 新单例 | 成员 | 归并到 | 理由 |
|---|---|---|---|
| `developer-tools` | builder-hub | essentials | 插件市场管理=平台元/基础设施,与 setup/memory 同类;消除 dev-tools/developer-tools 近义重复 |
| `legal-education` | cn-legal-study | showcase | 备考教育类,与 math-olympiad 同属「学习/展示」长尾;与 legal-workflow 实务工作流受众不同,不混入 |

---

## 2. 阶段一:分类归并(42 条改 category,33 条不变 → 11 域)

- 不变 33:language-server(12)、legal-workflow(11)、finance(7)、session-style(2)、security/crabcode-security-review(1)。
- 改动 42:dev-workflow(8)、dev-tools(9)、integrations(11)、content-office(3)、security 增(2)、essentials(5)、showcase(4)。
- 最终 11 域 / 75 条:language-server 12、legal-workflow 11、integrations 11、dev-tools 9、dev-workflow 8、finance 7、essentials 5、showcase 4、content-office 3、security 3、session-style 2。

### 阶段一执行结果 ✅
- dry-run 强校验:42 条 name 全部存在且 from-category 匹配。
- JSON round-trip 零格式噪音(diff 中非 category 行为空)。
- 写入后:`git diff` = 42 insertions + 42 deletions,纯 category 行;JSON 合法;改后 11 域 / 75 条分布与预期一致。
- 验证口径:目录/插件名/其它展示字段一律未动(仅 category 字段值变化)。

---

## 3. 阶段二:Knowledge-Work 转化 ✅(14 个 crabwork-*)

### 执行方式
- 主控亲手转 `crabwork-engineering` 样板 + 写统一规范 `/tmp/crabwork-conversion-spec.md`。
- 其余 13 个派 subagent 并行转化(第一批 4 个验证质量后,放量派 9 个,small-business 单独大任务)。
- 主控逐个独立复核(品牌残留/skills 有效/中文化/字段),不直接采信 subagent 结论。
- marketplace 14 条 + 4 新域由主控统一写入(避免并发写冲突)。

### 14 个插件(域)
- eng-data(2):engineering(10)、data(10)
- go-to-market(3):sales(9)、marketing(8)、customer-support(5)
- biz-ops(4):product-management(8+1 cmd)、hr(9)、design(7)、operations(9)
- work-platform(5):productivity(4)、enterprise-search(5)、plugin-management(2)、bio-research(6)、small-business(31)
- 合计约 123 skill。**small-business → work-platform**(方案二节遗漏,已补:避免单插件单独成域)。

### 复核发现并修正的真实问题(根因导向)
1. **运行时标识符不一致**:productivity / plugin-management 的 subagent 误保留 `${CLAUDE_PLUGIN_ROOT}` 与 `CLAUDE.md`。本仓既定约定为 `${CRABCODE_PLUGIN_ROOT}`(现有 plugin-dev/hooks 全用)+ `CRABCODE.md`(112 vs 残留 42)。
   - 根因:运行时标识符若不用本仓约定,生成的插件会失效。已统一 sed 修正 `CLAUDE_PLUGIN_ROOT→CRABCODE_PLUGIN_ROOT`、`CLAUDE.md→CRABCODE.md`、`claudeMd/claudeContent/claudeFileHandle→crabcode*`(8 文件)。
2. **LICENSE 文件有无不一**:部分插件被 `cp -r` 带入上游 LICENSE,plugin-management 则删除。按本仓惯例(现有插件均不带 LICENSE 文件,署名靠 plugin.json license + README 归属)统一删除全部 crabwork-* 的 LICENSE/LICENSE.txt(13 个)。

### 已知合法残留(非品牌泄漏,经判定保留)
- README 顶部归属链接 `knowledge-work-plugins`:14 处(Apache-2.0 署名)。
- 第三方真实服务 url:`mcp.hubspot.com/anthropic`(sales/marketing/customer-support/small-business)、`mcp.claude.com`(bio-research)等 8 处(规范禁改 url)。
- "coworking"(HR 共享办公术语,非 Cowork 品牌)。
- 意外品牌残留:**0**。

### 去品牌化口径(统一执行)
- plugin.json:name→crabwork-*、author CrabCode、version 0.1.0、+Apache-2.0、+keywords、+skills[](显式列举,office-suite 范式)。
- .mcp.json:删 slack oauth 块,保留全部 url(空 url 连接器原样)。
- README/CONNECTORS:去品牌 + 中文化 + 顶部归属行。
- SKILL.md:去品牌;正文保持英文(不翻译,避免范围爆炸);description 保持上游触发式结构。

### marketplace 注册 + 验证 ✅
- 总条目 75 → **89**;新增 4 域(eng-data/go-to-market/biz-ops/work-platform);全仓 11 → **15 域**。
- 全仓交叉验证:JSON 合法、无孤儿条目、14 目录全注册、plugin.json 字段+skills 路径全合规、展示字段齐全。

---

## 4. 阶段三:legal/finance 参考优化 ✅(仅产出建议,不改插件)

- 对照上游 legal(9)/finance(8)skill 骨架,审视现有 crablaw-cn(13 板块)/ crabfin-cn(7 板块)能力缺口。
- 产出 `docs/audit/2026-06-21-法财参考优化建议.md`:逐 skill 覆盖判定 + 缺口分级 + 本地化借鉴建议 + 优先级。
- 高价值缺口 2 项(matter-core 统一风险分级框架、cn-fund-admin 月末结账编排器);中价值 5 项;无需引入 6 项(已强覆盖)。
- **未修改 crablaw-cn / crabfin-cn 任何文件**(符合方案第六节约束)。

---

## 5. 收尾:crabwork-* skill-creator 深度优化(/goal 三项口径)

- **任务分支**: `task/skill-creator-optimization-20260621`(接续 main b710839)
- **项3(范围外 CLAUDE 残留)**:核实闭环。本仓源码层 0 残留;`grep` 命中的 3 文件全在
  `plugins/crabcode-media-ops/node_modules/bun-types/`(第三方 npm 依赖文档),按"不改他人依赖"原则不动。
- **项2(README/CONNECTORS 中文化方向)**:用户拍板**保留中文**,无改动。
- **项1(逐 skill skill-creator 优化)**:1 样板(crabwork-engineering)定标准 → 复核达标 → 13 个放量 subagent
  (各 1 插件,small-business 单独),共 **14 插件 / 123 skill**。subagent 不碰 git、不写 marketplace.json。
  - 范围:静态作者级优化(description 触发强化 + 正文渐进披露 + 大文件 references 抽取),正文保持英文。
  - 结果:**98 个 SKILL.md** description 触发强化;约 25 个已达标按防过度工程保留;
    **11 处 references 抽取**(2 个超 500 行红线:data/build-dashboard 924→156、sales/create-an-asset 867→497;
    customer-support/product-management/marketing 多个长文件渐进披露),内容零丢失、指针无断链。
  - 附带根因修复:small-business/job-post-builder 的 `references/`↔`reference/` 断链 + 指向不存在 tests/ 的死链节。
- **终点毁灭性复核审计**:0 品牌残留;改动全在 crabwork-* 内、未碰 marketplace/plugin.json/README/CONNECTORS/.mcp.json;
  123 个 frontmatter 合法(name=1 desc=1);无 SKILL.md 超 500 行;11 处 references 均有内容且指针解析通过。
- 合并 main 仍为破坏性终点操作,**留给用户裁决**。



