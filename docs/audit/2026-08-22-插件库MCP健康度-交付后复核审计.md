# 插件库 MCP 健康度交付后复核审计

> **2026-08-23 历史结论撤销**：本文的“通过，可提交、合并并推送”只记录 2026-08-22 已经发生的 Git 操作，不再是有效交付结论。深度复核证明官方镜像仍分发止血前字节、task tree 还含可执行模板 `.mcp.json`、测试记录未绑 tested commit/tree/log SHA，且 OAuth/release 合同有 P1 缺口。当前结论固定为 `blocked`，以《[插件库 MCP 深度复核审计与整改第一真源](./2026-08-23-插件库MCP深度复核审计与整改第一真源.md)》为准。
>
> 日期：2026-08-22（America/Los_Angeles）
>
> 审计对象：`mcp-emergency-safe-baseline-v1` 插件仓交付
>
> 复核结论：**历史 Git tree 通过；整体交付 blocked，不得据此文再发布通过结论**

## 1. 复核结论

本次 Git task/merge tree 按前置审计边界完成了 marketplace plugin root 的 remote=0 源码止血。它没有伪造 CrabCode generation=2、Acosmi 生产发布、供应商批准、WORM、HSM 或 release-lock 已完成；但官方镜像未发布该树，所以不能再把“Git tree 安全”写成“面向用户的止血已交付”。

复核后确认仍有 P0 分发缺口和 P1 门禁/发布/OAuth 缺口。历史 task tree 的 marketplace plugin root 只剩 `html-video`，但全仓还有 `templates/plugin-mcp-wrapper/.mcp.json`；“全仓可执行面只剩一个”的历史措辞不成立。

## 2. 机器不变量

| 项目 | 复核值 | 结论 |
| --- | ---: | --- |
| 已发布 plugin root `.mcp.json` | 1 | 历史 Git tree 通过 |
| task tree 全仓 `.mcp.json` | 2（html-video + template） | 历史漏检，blocked |
| 已发布 plugin root MCP server | 1 | 历史 Git tree 通过 |
| 已发布 plugin root HTTP / SSE | 0 / 0 | 历史 Git tree 通过 |
| 已发布 plugin root stdio | 1（`html-video`） | 历史 Git tree 通过 |
| task tree 全仓可执行 server | 2（`html-video` + template `npx@latest`） | blocked；整改后必须=1 |
| 非空 `requiredMcpServers` 插件 | 1（`html-video`） | 通过 |
| raw LSP `.mcp.json` | 0 | 通过 |
| 暂停配置 | 42 | 通过 |
| marketplace 暂停披露 | 42 | 通过 |
| 受影响插件一次 patch bump | 43/43 | 通过 |
| 自动 workflow trigger | 0 | 通过 |
| API 手动 workflow | 3/3 | 历史字面计数修正；仍需 canonical YAML 负测 |
| 官方镜像 `latest` | `2e0b1266dcc4c34f8930cd589ce7aaedd6aa0f10` | 不含 task `7975750`，P0 blocked |

历史不变量只覆盖了 `plugins/*/.mcp.json` 与粗粒度 workflow 文本；`mcpContractValidator`、`workflowTriggerValidator` 和 `mcp-safe-baseline.test.ts` 当时未 hard fail repository-root/nested marketplace root、manifest inline/path MCP、MCPB、template `.mcp.json` 或 quoted YAML event。机器证据合同已在 2026-08-23 升级为 schema v2，但 `testedCommit/testedTree/log SHA` 仍待整改重跑填充。

## 3. 复核中发现并已修正的问题

1. 原方案正文仍残留固定 `127.0.0.1:3118`，与动态 loopback 裁决冲突。文档改为静态 `http://127.0.0.1/callback` + OS-assigned 实际端口后，当时错误地把它写成普适合同。2026-08-23 修正为候选默认：只在 AS/provider 证明 RFC 8252 port exception 后可用，并必须补 MCP `2026-07-28` `iss/resource/S256`、逐 provider E2E 与 localhost 应用冒充风险证据；canonical CIMD URL 不变。
2. `html-video` serverInfo 原先硬编码 `0.1.0`。现从 package version 派生并以 MCP 客户端断言 `0.1.3`。
3. `mediaops` 升至 `0.4.3` 后，runtime、README、SBOM 和自有 validator 仍保留 0.4.2/自动激活合同。现已全部对齐；其 validator 明确要求 `.mcp.json` 不存在、`requiredMcpServers` 为空。
4. 删除旧远程配置后，5 条品牌白名单变成陈旧豁免。现已删除，仅保留真实物种学名假阳性。
5. `cn-legal-study` 当时的“既存漂移”判断不实：真实 parent `0192ec8` 中 manifest/marketplace 均为 `0.2.1`，本 MCP task 将它改到 `0.3.0` 是范围污染。版本已进 main，不再降级；只修正审计事实，不继续升版。
6. 原工作树含用户既存 crabcopyright 大量修改；该工作已由独立提交 `ecbe2d2` 先行合入远端主线。本交付在独立 worktree 重放到最新 `origin/main`，确认 MCP 提交差异未重复混入 crabcopyright 文件。

## 4. 本机测试复核（历史、未绑定）

以下是 2026-08-22 记录的历史结果，未触发 GitHub Actions；但原 evidence 未包含 `testedCommit/testedTree`、每条命令 exit code/开始结束时间或 log path/size/SHA-256，因此它们不再有资格支撑当前合并/发布门。整改树必须重跑并填充新 evidence 合同。

- 基于最新 `origin/main=0192ec8` 的隔离 task-only 根仓：257 pass、0 fail；typecheck/build/brand/validate/actionlint 全部通过；
- `html-video`：44 pass，真实 MCP initialize/tools-list/tool call、Chromium 安全测试、dist-only cold start 和 doctor 通过；
- `mediaops`：109 核心测试 + 1 强 QA + 8 发布浏览器测试通过；
- `media-publisher`：25 单测、15 份 HTML Nu 校验、39 浏览器流程通过；
- `crabcode-security`：505 provenance checks、完整 port patch 重放、88 测试通过；
- 主线已独立合入的 crabcopyright：历史记录为 11 插件测试；最终树强 PDF 仓级测试数应为 **14**，不是 13。它不属本 MCP 功能差异，整改证据需以 14 重跑/填充。

`validate` 还报告 1 条 crabfin capability-route warning 与 30 条主线既存 doc-facts warning；均未触及本提交文件、未被本次降级或新增，且当前政策定义为非阻断 warning。

历史机器记录见 `evidence/2026-08-22-mcp-health/local-test-results.json`。锁定 Playwright Linux 镜像、Python 3.9 与 Python 3.13 均为 `not-tested`；它们在 2026-08-23 整改门中是 blocker，不得再由说明文字降为 non-gate。

## 5. 关联方与剩余阻断

- Acosmi 仓的 contract-baseline 已用 SHA-256 绑定本仓两份合同与 PR `#21`/task/merge；canonical CIMD URL 保持 `https://acosmi.com/oauth/crabcode-client-metadata`。静态 loopback 注册值是候选 provider 策略，不是普适保证。A/B 只受 `ACOSMI-REL-BASE` 阻断；`P0-0A-BOOTSTRAP` 只阻断 `GENESIS-INTEGRATION`/future lock。
- 任何远程连接器恢复仍需独立完成宿主安全 loader、真实认证/reconnect/tools-list/canary 和适用供应商资格；本提交不授权恢复。
- 官方镜像 2026-08-23 仍分发旧提交 `2e0b1266...`，所以旧安装尚无可用的安全升级字节。镜像完成手动发布后，客服/发布说明必须要求“升级插件并重启 CrabCode”；宿主 targeted eviction 上线前不宣称热更已止血。旧 token 不由本仓删除。
- `mediaops` 治理工具在恢复前必须显示 `MCP_INACTIVE` / `GATE_NOT_EXECUTED`，不能把手工替代标为已治理。
- 12 个 LSP 删除的是伪 MCP 声明，不代表 LSP 产品化完成。

## 6. 最终裁决（2026-08-23 更正）

历史 Git task/merge 已完成，但整体交付不满足当前门禁。下列 2026-08-22 顺序只是已发生的历史记录，不是当前授权：

```text
提交 task-only 变更
→ 复核提交树与测试证据
→ 合并至 main
→ 推送任务分支与 main
→ 不自动触发 CI
```

当前只允许按 2026-08-23 第一真源完成整改、重跑并绑定本机证据，再通过 API 依次手动触发 exact-main CI、mirror publish 与 mirror audit；公网 `latest/ZIP/checksum/marketplace` 及旧→新安装+重启未通过前状态为 blocked。未来 Acosmi 或 CrabCode 交付完成不会自动改变本仓 blocker；恢复任何 profile 必须另起经审计变更。
