# 插件库 MCP 健康度交付后复核审计

> 日期：2026-08-22（America/Los_Angeles）
>
> 审计对象：`mcp-emergency-safe-baseline-v1` 插件仓交付
>
> 复核结论：**通过，可提交、合并并推送**

## 1. 复核结论

本次交付按照前置审计纠偏后的边界完成了插件仓可独立证明的安全止血。它没有伪造 CrabCode generation=2、Acosmi 生产发布、供应商批准、WORM、HSM 或 release-lock 已完成；这些事项继续阻断未来远程 profile 解锁，但不再阻断当前 remote executable=0 的安全基线。

复核未发现未解决的 P0/P1 级插件仓问题。所有可执行 MCP 发布面只剩一个已随包、可离线冷启动并通过真实握手/工具调用的 `html-video` stdio sidecar。

## 2. 机器不变量

| 项目 | 复核值 | 结论 |
| --- | ---: | --- |
| `.mcp.json` 文件 | 1 | 通过 |
| MCP server | 1 | 通过 |
| HTTP / SSE | 0 / 0 | 通过 |
| stdio | 1（`html-video`） | 通过 |
| 非空 `requiredMcpServers` 插件 | 1（`html-video`） | 通过 |
| raw LSP `.mcp.json` | 0 | 通过 |
| 暂停配置 | 42 | 通过 |
| marketplace 暂停披露 | 42 | 通过 |
| 受影响插件一次 patch bump | 43/43 | 通过 |
| 自动 workflow trigger | 0 | 通过 |
| API 手动 workflow | 2/2 | 通过 |

这些不变量由 `mcpContractValidator`、`workflowTriggerValidator` 和 `mcp-safe-baseline.test.ts` 共同 hard fail；机器证据见 `evidence/2026-08-22-mcp-health/containment-inventory.json`。

## 3. 复核中发现并已修正的问题

1. 原方案正文仍残留固定 `127.0.0.1:3118`，与前置审计动态 loopback 裁决冲突。主方案与 Acosmi 独立方案现均改为 CIMD portless 注册基值 `http://127.0.0.1/callback`，宿主实际授权使用 OS-assigned loopback 端口；canonical CIMD URL 不变。
2. `html-video` serverInfo 原先硬编码 `0.1.0`。现从 package version 派生并以 MCP 客户端断言 `0.1.3`。
3. `mediaops` 升至 `0.4.3` 后，runtime、README、SBOM 和自有 validator 仍保留 0.4.2/自动激活合同。现已全部对齐；其 validator 明确要求 `.mcp.json` 不存在、`requiredMcpServers` 为空。
4. 删除旧远程配置后，5 条品牌白名单变成陈旧豁免。现已删除，仅保留真实物种学名假阳性。
5. 工作树既存 `cn-legal-study` marketplace=`0.3.0` / manifest=`0.2.1` 漂移已以最小方式对齐至 `0.3.0`，未制造无意义的第二次 patch bump。
6. 原工作树含用户既存 crabcopyright 大量修改；该工作已由独立提交 `ecbe2d2` 先行合入远端主线。本交付在独立 worktree 重放到最新 `origin/main`，确认 MCP 提交差异未重复混入 crabcopyright 文件。

## 4. 本机测试复核

所有合并门均在本机执行，未触发 GitHub Actions：

- 基于最新 `origin/main=0192ec8` 的隔离 task-only 根仓：257 pass、0 fail；typecheck/build/brand/validate/actionlint 全部通过；
- `html-video`：44 pass，真实 MCP initialize/tools-list/tool call、Chromium 安全测试、dist-only cold start 和 doctor 通过；
- `mediaops`：109 核心测试 + 1 强 QA + 8 发布浏览器测试通过；
- `media-publisher`：25 单测、15 份 HTML Nu 校验、39 浏览器流程通过；
- `crabcode-security`：505 provenance checks、完整 port patch 重放、88 测试通过；
- 主线已独立合入的 crabcopyright：11 插件测试、13 强 PDF 仓级测试通过；不属于本次 MCP 提交差异。

`validate` 还报告 1 条 crabfin capability-route warning 与 30 条主线既存 doc-facts warning；均未触及本提交文件、未被本次降级或新增，且当前政策定义为非阻断 warning。

完整机器记录见 `evidence/2026-08-22-mcp-health/local-test-results.json`。锁定 Playwright Linux 镜像补充复测因 MCR layer EOF/停滞未完成；它不是合并门，且没有远程 CI 结果被用于本结论。

## 5. 关联方与剩余阻断

- Acosmi 任务已收到机器合同：canonical CIMD URL 保持 `https://acosmi.com/oauth/crabcode-client-metadata`，JSON 使用 portless loopback 注册值；A/B safe deployment 与 P0-0A/global lock 已拆分。
- 任何远程连接器恢复仍需独立完成宿主安全 loader、真实认证/reconnect/tools-list/canary 和适用供应商资格；本提交不授权恢复。
- 已安装旧版本插件只有在升级后才取得安全字节；客服/发布说明必须要求升级。旧 token 不由本仓删除，但安全版本不发送它们。
- `mediaops` 治理工具在恢复前必须显示 `MCP_INACTIVE` / `GATE_NOT_EXECUTED`，不能把手工替代标为已治理。
- 12 个 LSP 删除的是伪 MCP 声明，不代表 LSP 产品化完成。

## 6. 最终裁决

插件仓交付满足前置审计合并门，且 task-only 干净树复验通过。允许按以下顺序收口：

```text
提交 task-only 变更
→ 复核提交树与测试证据
→ 合并至 main
→ 推送任务分支与 main
→ 不自动触发 CI
```

未来 Acosmi 或 CrabCode 交付完成不会自动改变本仓 blocker；恢复任何 profile 必须另起经审计变更。
