# 执行日志:仓库结构 ↔ 下游「管家工作流」两层模型对齐(A2 + B1)

- **日期**: 2026-06-21
- **任务分支**: `task/workflow-model-alignment-20260621`(接 main `5b3065a`)
- **方案基准**: `docs/audit/2026-06-21-工作流模型对齐审计与实施方案.md`(自包含,顶部 ★ 节存原始需求)
- **目标**: A2(大域即单工作流、板块降为 `groups`)+ B1(marketplace 条目加 `tier`)

---

## 0. 前置审计(从 marketplace.json + 磁盘重新推导,不采信文档旧数字)

### 实况核实
- marketplace 条目 **89** / 目录 **70** / category **15**;条目无 `tier` 字段(确认根因①)。
- `plugins/crablaw-cn`、`plugins/crabfin-cn` **均无顶层 plugin.json**(确认根因②:纯容器目录,装不到"一个主插件")。
- crablaw-cn 下 **13 个子目录**,各有自己的 `.crabcode-plugin/plugin.json`;crabfin-cn 下 **7 个子目录**同构。
- 全 18 容器子插件 **commands=0、各自带 skills[]**;法律 105 skill / 财务 54 skill 与文档一致。

### ★ 关键偏差发现(文档与实况不符 → 主控修订)
文档把 crablaw-cn 当"13 板块全是法律"。实况:更早的「分类归并」任务(已提交,在本分支祖先链上)已把其中 **2 个移出 legal 类别**:
- `cn-legal-study`(11 skill,category=**showcase**):desc="PRC legal study and bar/postgraduate exam preparation aids" → 备考教育,**非法律实务**。
- `builder-hub`(8 skill,category=**essentials**):desc="Plugin and skill management meta-tooling" → 平台元工具,**与法律无关**。

两者物理上仍嵌在 `plugins/crablaw-cn/` 下,但类别与性质都不属于法律实务。

### 偏离登记(第三阶段第 2 条 · 架构/范围)
- **偏离点**:法律工作流伞的边界从文档的「13 板块/105 skill」修订为「**11 实务板块/86 skill**」;`builder-hub`、`cn-legal-study` **提取为顶层独立插件**(`git mv` 到 `plugins/builder-hub`、`plugins/cn-legal-study`),不并入法律 `groups`。
- **原因**:
  1. 尊重已提交的分类归并决策(showcase/essentials),不得静默覆盖其他工作的改动(红线)。
  2. 若把它们留在 `plugins/crablaw-cn/` 下又让该目录成为伞插件,则形成"伞插件内再嵌 2 个独立插件"的病态——正是根因②要消除的"目录既是插件又是容器"中间态。提取才是根因解。
  3. 下游「法律」预设的能力卡应是法律实务能力;混入"插件构建器/备考"会误标并污染该工作流(违背下游两层模型语义)。
- **影响面**:marketplace 这两条 `source` 路径改写;它们成为独立可安装插件(tier 见下)。无其它文件引用其旧路径(已 grep 核实,仅 marketplace.json 两处)。skill 内容零改(git mv 保留)。
- **与原始需求一致性**:A2 的"法律大域=单工作流"指**法律实务大域**;提取非法律项使伞更纯、更符合"装一个法律工作流"的本意,比文档字面的"13 板块"更忠实。属打包层重构,未引入新依赖、未改 skill 内容,范围未扩大。
- **默认保守校验**:通过——本修订使改动**更贴合**已落地的分类与下游语义,且可完全由 git mv + JSON 改写回退。

### skill 名唯一性(下游 token 唯一性前置验证)
- 法律 11 实务板块 86 skill:**名全唯一**,0 重复。
- 财务 7 板块 54 skill:**名全唯一**,0 重复。
→ 可安全合并入单伞的扁平 token 空间。

### 三问结论(复核文档审计,成立)
1. **架构偏差**:是(根因①无 tier 分层 + 根因②工作流三层物理深度并存)。已核实属实。
2. **过度工程**:局部(大域 skill 过密),根因解=分层暴露(`groups`),非删内容。成立。
3. **遗漏同类/低估关联方**:文档已纠正 06-20 审计的低估;本执行日志再纠正文档自身对 crablaw-cn 边界的一处误判(见上偏差)。

---

## 1. tier 机械分类(规则 + 全条目归属)

规则:components = skills+commands+agents,≥3 → **workflow**;主资产为 `.mcp.json`/LSP 桥接、自有 skill/cmd ≤ 薄封装 → **connector**;单一能力/样式/示例/安装器 → **capability**。

收敛后 73 条目分布:
- **workflow(29)**:crablaw-cn、crabfin-cn、builder-hub、cn-legal-study、crabcode-office-suite、crabcode-media-ops、14× crabwork-*、plugin-dev、code-modernization、pr-review-toolkit、feature-dev、mcp-server-dev、agent-sdk-dev、hookify、commit-commands、ralph-loop。
- **connector(27)**:12× *-lsp、asana、context7、firebase、github、gitlab、greptile、linear、playwright、serena、terraform、laravel-boost、discord、telegram、imessage、fakechat。
- **capability(17)**:crabcode-setup、crabcode-security-review、crabcode-memory-management、code-review、code-simplifier、cwc-makers、crabcode-example-plugin、frontend-design、math-olympiad、playground、session-report、skill-creator、security-guidance、explanatory-output-style、learning-output-style、crabcode-example-skills、ai-api-dev。

条目数推算:89 − 11(法律实务子条目并入伞) − 7(财务子条目并入伞) + 2(两伞) = **73**。

---

## 2. 实施记录

### S2a 迁移非法律目录(git mv,内容零改)
- `plugins/crablaw-cn/builder-hub` → `plugins/builder-hub`;`plugins/crablaw-cn/cn-legal-study` → `plugins/cn-legal-study`。
- git 记录为 rename(skill 内容、各自 plugin.json 的 `./skills/*` 相对路径不变,迁移后命中验证:builder-hub 8/8、cn-legal-study 11/11)。
- crablaw-cn 剩 11 法律实务板块。

### S2b 建顶层伞 + 降级板块
- 新建 `plugins/crablaw-cn/.crabcode-plugin/plugin.json`(skills[] **86** 条,路径 `./<板块>/skills/<skill>`,板块原地不动)。
- 新建 `plugins/crabfin-cn/.crabcode-plugin/plugin.json`(skills[] **54** 条)。
- 伞 skills[] 路径 100% 命中真实 SKILL.md(86/86、54/54)。
- `git rm` 全部 18 个板块级 `.crabcode-plugin/`(板块降级为伞下纯 skill 载体,不再独立注册);板块 `.crabcode-plugin/` 此前仅含 plugin.json,无其它资产丢失。

### S1 重写 marketplace.json
- 全 73 条目加 `tier`;法律 11 + 财务 7 子条目收敛为 2 个带 `groups` 的伞条目;builder-hub/cn-legal-study `source` 改写为顶层路径。
- 条目数 89 → **73**;tier 分布 **workflow 29 / connector 27 / capability 17**。
- 伞条目含完整展示字段(displayName「法律工作流」「财务分析工作流」+ shortDescription/longDescription/defaultPrompt/brandColor)+ `groups[]`(每板块 name/displayName 取自原子条目/skills 列表)。

### S3 根因④:多组件伞补 skills[] 声明
- `mcp-server-dev`(+3)、`plugin-dev`(+7)、`crabcode-media-ops`(+3)补 `skills[]`(与磁盘目录核验一致)。
- media-ops/plugin-dev 的 agents 属内部实现,下游卡片无 agent token 类型,按模型不进卡片层,保留不声明为卡片。
- hookify 用 `skills:"./skills"`(目录指针)合法,保留。

### S4 全仓交叉核对(机械,非抽样)
- 73 条目 ↔ 73 磁盘 plugin 目录**完美双射**;每条 source 目录存在且含 plugin.json 且 manifest.name==条目名。
- 每个 workflow 伞的 `groups.skills` 与磁盘 SKILL.md 及伞 `skills[]` **三方一致**。
- tier 三类全覆盖无 None;name 全唯一。
- 法律/财务各自 skill 名全唯一(下游 token 唯一性)。

### S5 验证与回归(Phase-4,真实输出)
- `bun run lint:marketplace` → **exit 0**(无输出)。
- `bun run lint:manifest` → **exit 0**。
- `bun run lint:layout` → **exit 0**。
- `bun run lint:brand` → exit 1,但**全部命中在未触碰的 crabwork-* 文件**(knowledge-work Apache-2.0 归属链接、第三方 URL `mcp.hubspot.com/anthropic`/`mcp.claude.com`、`Xenopus→opus`/模型名假阳性)。品牌命中 ∩ 本次改动文件 = **空集**。
- `bun test ./tests/` → **40 pass / 1 fail**;唯一 fail 为 `brand guard`(233 命中,同上品牌数据)。
- **干净 main worktree 对照**:`bun test` 同为 **40 pass / 1 fail(brand guard,233 命中完全相同)** → 该失败**前存于 main,非本次引入**;本次品牌命中增量 = 0。
- typecheck:本次仅改 JSON,未触碰任何 `.ts`,typecheck 结果与 main 等同(未引入类型变更)。

### 需求逐条对齐(原始需求清单)
| 需求 | 状态 | 说明 |
|---|---|---|
| A2:大域即单工作流 | ✅ 满足 | crablaw-cn(11板块/86skill)、crabfin-cn(7板块/54skill)各收敛为 1 个可安装伞工作流条目 |
| A2:板块降为 groups | ✅ 满足 | 伞条目 `groups[]` 携带板块 name/displayName/skills,驱动下游"卡片按板块分组"与"市场内分组显示" |
| B1:加 tier 字段 | ✅ 满足 | 全 73 条目带 tier(workflow/connector/capability) |
| 根因②:消除纯容器中间态 | ✅ 满足 | 两容器获顶层 plugin.json 成可安装伞;板块级 plugin.json 移除,不再"目录既是插件又是容器" |
| 根因④:多组件伞声明能力 | ✅ 满足 | 3 个缺声明插件补 skills[] |
| 红线:只动打包层不碰 skill 内容 | ✅ 满足 | git diff 中 SKILL.md 仅为 rename(0 内容变更) |
| 红线:不覆盖其他工作 | ✅ 满足 | 尊重前任务分类(builder-hub/cn-legal-study 保持 essentials/showcase),git mv 保留内容与历史 |
| 红线:破坏性 git 交用户 | ✅ 待终点 | 合并 main/push 留作终点闸门 |
| 跨端契约:tier/groups 字段名需下游确认 | ⚠️ 部分 | 本仓已产出可消费数据并以 docs 记录契约;**落地前需与下游对齐字段命名**(偏离登记已标) |



## 3. 终点毁灭性复核审计(敌意视角)

### 同类变体扫描(机械)
1. **容器模式变体**:扫描 plugins/ 下"无顶层 plugin.json 却含子插件"的目录 → **0 个**。crablaw-cn/crabfin-cn 是仅有的两个,均已根治 → 根因②容器模式已根除,无其它模块复发。
2. **卡片溢出变体(根因③)**:tier=workflow 且 skill>12 且无 groups 的 → **仅 `crabwork-small-business`(31 skill)**。
3. **漏声明变体(根因④)**:tier=workflow 有磁盘 skill 却未声明 skills[] 的 → **0 个**(无漏网)。

### ★ 已知缺口 → 已闭合(2026-06-21 后续追加,提交 `580d89b`)
- **原缺口**:`crabwork-small-business`(31 skill,无 groups)= 根因③同类变体,初版未覆盖。当时不处理的判断(偏离第3条·默认保守):A2 决策只圈定法律/财务;S1–S6 未含;且 small-business 是单层 31 扁平 skill、无天然子板块,分组需发明语义分类(产品判断,非纯打包动作),自动聚类反成过度工程,故显式登记而非静默跳过。
- **闭合动作**(用户后续明确授权处理,要求"能力专注、不强行合并、合理的除外、参考法律/财务,下游不必一屏"):
  - 按语义把 31 skill 归为 **11 个能力专注子板块**(现金流与薪酬/月度结账/税务申报/定价与盈利/销售管道/营销与内容/客户关系与CRM/客户服务/合同与用人/经营简报与复盘/上手与智能路由),`groups[]` 字段与法律/财务伞同构。
  - **唯一合理合并**:`job-post-builder`(招聘)并入"合同与用人"——消除单卡片退化板块,且与合同同属后台文书/用人事务,焦点一致;其余均保持独立专注(含 2 卡的税务/定价/客户服务,因下游不要求一屏)。
  - **验证**:groups 内 31 skill 与磁盘 31 目录**完美双射**(无幽灵/无漏网/无重复);marketplace/manifest/layout 校验全 exit 0;JSON 合法;diff 单文件单 hunk 100+/1−。
  - **复发点(新)**:若今后再新增 `tier=workflow` 且 skill 数超一屏阈值却无 groups 的条目即复发;检测口径=扫 marketplace 中 tier=workflow & skills 多但缺 groups 的条目。

### 根因证伪小结
- 根因①(tier):反证——不加则每新增插件都要改下游硬编码映射,在"自定义工作流热插拔"处复发。已加 → 自描述归位。**残余依赖**:下游须实际消费 `tier`/`groups` 字段名(跨端契约,偏离登记已标,落地前需对齐)。
- 根因②:反证——保留 13/7 并列条目则"装一个法律工作流"不成立。已收敛为单伞 → 成立;变体扫描确认无其它容器。
- 根因③:反证——不分组则 86/54 卡溢出。法律/财务已 groups;**small-business 变体已于后续提交 `580d89b` 补 11 子板块 groups 闭合(见上),根因③同类变体现已全覆盖**。
- 根因④:反证——不补 skills[] 则 media-ops 类能力对下游不可见。3 个已补,无漏网。

### 交付完整性
- 核心产出均在 HEAD:`574b406`(实施)、`a274693`(文档)。工作树干净。
- 回归:marketplace/manifest/layout 校验 exit 0;brand guard 失败为 main 前存(增量 0);bun test 与 main 等同(40 pass/1 fail)。

### 结论
**通过(0 已知缺口)**:A2+B1 原始需求全部满足且经机械验证;原唯一未覆盖项 `crabwork-small-business` 分组已于后续提交 `580d89b` 补齐(11 能力专注子板块,双射验证通过),根因③同类变体现已全覆盖。破坏性 git(合并 main/push)留终点闸门交用户。

> **后续追加提交**(本任务分支,初版交付之后):`580d89b` — small-business 补 groups。终点闸门(合并 main/push)依旧默认不执行,留交用户裁决。
