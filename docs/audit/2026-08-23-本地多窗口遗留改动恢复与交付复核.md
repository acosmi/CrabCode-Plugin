# 本地多窗口遗留改动恢复与交付复核

- 日期：2026-08-23
- 仓库：`acosmi/CrabCode-Plugin`
- 当前基线：`origin/main@c1c5dd9cd3494af783ce98f49a58e9ba361638aa`
- 恢复分支：`codex/crabfin-audit-xls-routing`
- 结论：**本机恢复与当前基线验证通过；等待 PR #22 由管理员先行合并后，必须在 exact-main 上复验，届时才可转 Ready。**

## 1. 大规模未提交状态的根因

原工作区位于已合并任务的旧分支 `codex/crablaw-legal-skills-research-plan`，同时保留了 CrabLaw、CrabCopyright、MCP 止血和 MCP 深审多个窗口的中间工作副本。它不是一项可以整体提交的功能变更。

按工作区字节生成 Git blob 并与 `origin/main`、CrabCopyright 最终分支、MCP 深审最终分支及其检查点逐文件比对：

| 项目 | 数量 | 裁决 |
|---|---:|---|
| tracked dirty | 182 | 纳入对象级比对 |
| untracked files | 63 | 纳入对象级比对 |
| 与当前 `main` 完全一致（含相同删除状态） | 223 | 已交付内容的工作副本，不重复提交 |
| 与旧检查点完全一致 | 1 | 已被最终交付提交取代 |
| 不等于任何最终树的早期草稿 | 21 | 均位于 CrabCopyright/MCP 已重写路径，不形成第三组独立功能 |

为避免误把过时实现重新带回，同时保留可恢复性，原现场保存为本地 stash：

```text
stash commit: a598989472fe34c4f99c73115e02cb398c1a9d73
message: pre-cleanup mixed workspace snapshot 2026-08-23; superseded by PRs 18,20,21,22
```

该 stash 未推送，不进入发布树。原工作区随后切回干净 `main`。在 PR #22 和本恢复分支完成 exact-main 复核前不删除该安全快照。

## 2. 两个隔离 worktree 的误删

`CrabCode-Plugin-wt-validator-audit` 与 `CrabCode-Plugin-wt-crabfin-routing` 都只出现以下四个无关构建产物删除：

```text
plugins/crabcode-html-video/dist/bootstrap.js
plugins/crabcode-html-video/dist/hyperframe.manifest.json
plugins/crabcode-html-video/dist/hyperframe.runtime.iife.js
plugins/crabcode-html-video/dist/server.js
```

历史执行日志已证明这些删除在相邻任务开始前就存在且不属于两项任务。现已从各自 `HEAD` 精确恢复；未恢复其他路径，也未覆盖任务改动。

## 3. 真实未交付成果

### 3.1 CrabAccount validator 敌意复核记录

- 原提交：`6c332011d8e649eedc585f9ffb68ef2d3eb72ab6`
- 当前基线重放提交：`437ad52`
- 产品改动：无
- 交付内容：对下游 CrabCode `fea30c131` 的调用链、攻击夹具、残余缺口和跨平台证据边界审计
- 关联方边界：本仓只归档审计事实，不修改、不推送、不合并下游 CrabCode 分支

### 3.2 CrabFin 工作簿审计能力路由

- 原提交：`1659a1ac29b9d763d78e163d63107dced4d43b2d`
- 当前基线重放提交：`18b5d08`
- 产品文件：`plugins/crabfin-cn/fin-core/skills/audit-xls/SKILL.md`
- 裁决：真实工作簿路由到已登记的 `crabcode-office-suite:crabcode-spreadsheets`；原文件只读；元数据不得冒充单元格审计证据；provider 缺失时只允许审计用户直接粘贴的公式或值
- 未扩大范围：不增加强依赖、不修改 Marketplace/manifest/能力注册表、不虚构重算能力

本轮遵循 `skill-creator` 的现有技能改进原则。该修复是确定性的单一能力路由缺口，仓库已有引用校验、模型正文哈希和全仓测试，因此复用现有客观门禁，没有为一个六行路由修复扩建多智能体主观评测工作区。

## 4. 当前基线验证

在重放后的 `origin/main@c1c5dd9` 树执行：

```text
bun install --frozen-lockfile                         PASS
bun run lint:refs                                    PASS，原 CrabFin warning 清零
bun test tests/workflow-skill-presentation-completeness.test.ts
                                                     1 pass / 0 fail / 1303 expect
bun test ./tests/                                    257 pass / 8 intentional skip / 0 fail
bun run validate                                    PASS（仅输出既有 doc-facts warnings）
bun run typecheck                                   PASS
bun run build                                       PASS
actionlint .github/workflows/*.yml                  PASS
git diff --check origin/main...HEAD                 PASS
```

调用身份仍为 316 个，调用集合 SHA-256 未变；CrabFin 路由后的当前模型内容 SHA-256 为：

```text
da69c588a97718e75d5e9dee4f5bd500e96c00b3aa26533fb47b212dbc3a103c
```

## 5. 合并顺序与硬门

PR #22 会修改同一个全仓模型正文哈希测试，因此本恢复分支不能在 #22 之前转 Ready 或合并，否则会制造可预见的集成冲突。正确顺序是：

1. 管理员审阅并合并 PR #22；
2. 本分支 rebase 到该 exact-main；
3. 重新计算模型正文哈希；
4. 重跑本节全部本机门；
5. 更新本报告的 exact-main 证据并将本 PR 转 Ready；
6. 仍由管理员执行最终合并。

本分支不修改工作流，不触发自动 CI，不发布镜像，不改变 MCP profile。系统级 `remote=0` 继续保持 fail-closed。
