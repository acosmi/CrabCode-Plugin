// Replace this stub with the plugin's real entry point.
// Standard plugins expose runtime logic from `src/` and surface declarative
// metadata via `.crabcode-plugin/plugin.json`.

export function placeholder(): string {
  return "replace me";
}
